{% extends 'layoutAdmin.html.twig' %}

{% block title %}Ajouter une formation{% endblock %}

{% block body %}
    <h1 class="training-title p-4 text-center">Créer une nouvelle formation</h1>

<div class="table-title container pt-lg-4 mb-lg-4">
    <div class="col text-center">
    {{ include('admin/training/_form.html.twig') }}
    </div>
    <div class="text-right ">
        <a class="btn btn-primary" href="{{ path('training_index') }}"><i class="fas fa-arrow-left icon"></i> Retour à la liste</a>
    </div>
</div>

{% endblock %}
