{% extends 'layoutAdmin.html.twig' %}

{% block title %}Modifier une réponse{% endblock %}

{% block body %}
<h1 class="training-title p-4 text-center">Modifier la Réponse</h1>

<div class="table-title container pt-lg-4 mb-lg-4">

    <div class="col text-center">
    {{ include('admin/answer/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
    </div>
<div class="text-right">
    <a class="btn btn-primary" href="{{ path('question_index') }}"><i class="fas fa-arrow-left icon"></i> Retour à la liste</a>
</div>
</div>
{% endblock %}
