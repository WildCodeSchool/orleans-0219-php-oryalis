{% extends 'layoutAdmin.html.twig' %}

{% block title %}Ajouter un membre{% endblock %}

{% block body %}
    <h1 class="training-title py-4 text-center">Ajouter un membre</h1>

    <div class="form-title container pt-lg-4 mb-lg-4">
        <div class="col">
            {{ include('admin_employee/_form.html.twig') }}
        </div>
        <div class="text-right">
            <a class="btn btn-primary" href="{{ path('admin_employee') }}"><i class="fas fa-arrow-left"></i> Liste des membres</a>
        </div>
    </div>

{% endblock %}
